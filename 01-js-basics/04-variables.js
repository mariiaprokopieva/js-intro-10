let myFavoriteBook = 'Amok'

// You can use digits in your variables anywhere but the first character

//You can not use special characters in your variables
//The only special characters allowed are underscore _ and currency ($...)

// You cannot use reserved keywords as your variables names but you can put underscore
// let class = 'Math' // Error: Compiler Error

let _class = 'Math'

let name1, name2, name3;

const firstDay = 'Sunday', secondDay = 'Monday', thirdDay = 'Tuesday';

let num1 = 10, num2, num3 = 25.5, num4;